name: flutter_time_utils_example
description: Demonstrates all features of the flutter_time_utils package.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_time_utils:
    path: ../
